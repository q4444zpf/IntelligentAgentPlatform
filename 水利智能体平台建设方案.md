# 通用智能体平台建设方案

## 1. 平台定位

建设一个面向多行业、多系统、多终端的通用智能体平台，支撑智能体创建、提示词管理、Skill / Tool 管理、知识库问答、流程编排、多智能体协同、专有 AI 对话、多用户访问、大模型接入、第三方系统集成和 Web / 桌面 / 客户端嵌入。

平台第一阶段先聚焦通用智能体能力底座，不绑定具体行业业务，不预设特定领域模型。后续可在平台底座之上接入政务、企业运营、工程管理、客服、办公自动化、数据分析、知识问答、业务辅助决策等行业场景。

核心目标：

- 统一管理智能体、Prompt、Skill、Tool、知识库、流程和大模型配置。
- 支持 Web 端、桌面端、嵌入式 AI 对话框、OpenAPI 和 SDK 等多种访问方式。
- 支持智能体按运行端、用户角色、业务系统和上下文来源进行差异化配置。
- 支持确定性工具调用、RAG 检索增强、多轮对话、任务执行和人工确认。
- 支持拖拽式流程编排，实现数据、工具、智能体、条件判断和人工节点组合。
- 支持企业级权限、租户隔离、审计日志、调用统计、监控告警和安全治理。
- Web 版智能体采用沙箱模式执行 Tool、MCP、脚本和文件处理任务，防止用户自建能力影响服务器安全。

## 2. 总体架构

平台建议采用“用户访问层 + 应用管理层 + 智能体能力层 + 工具与知识层 + 数据与集成层 + 基础设施层”的分层架构。

### 2.1 用户访问层

- Web 管理端
- 业务工作台
- 嵌入式 AI 对话框
- 桌面客户端
- 移动端 / 小程序
- 大屏系统
- 第三方系统 API / SDK 接入

### 2.2 应用管理层

- 智能体管理
- Prompt 管理
- Skill / Tool 管理
- 知识库管理
- 拖拽式流程编排
- 多智能体协同
- 专有对话管理
- 大模型设置
- 用户、角色、部门、租户和权限管理
- 系统集成与开放接口管理
- 审计、统计和运营管理

### 2.3 智能体能力层

- Agent 运行时
- Agent 调度引擎
- Tool / Skill 调用引擎
- Web 智能体沙箱执行引擎
- 多智能体协同引擎
- RAG 检索增强
- Prompt 组装与版本控制
- 对话上下文管理
- 大模型调用路由
- 任务规划与执行
- 人工审核与人机协同
- 安全策略与输出治理

### 2.4 工具与知识层

- 内置 Tool
- 自定义 Skill
- 外部 API 工具
- 文件处理工具
- 数据查询工具
- 内容生成工具
- 消息通知工具
- 业务系统动作工具
- 文档知识库
- 结构化知识库
- 向量索引与检索服务

### 2.5 数据与集成层

- 用户与组织数据
- 系统配置数据
- 对话与消息数据
- 工具调用数据
- 流程运行数据
- 文件与附件数据
- 知识库文档数据
- 第三方系统数据
- API 网关与 Webhook
- 事件总线与消息队列

### 2.6 基础设施层

- API 网关
- 容器化运行环境
- 消息队列
- 对象存储
- 关系型数据库
- 缓存数据库
- 向量数据库
- 日志、监控和审计
- 统一认证与权限体系
- 密钥管理与配置中心

## 3. 智能体管理

智能体是平台能力的核心组织单元。一个智能体由角色定位、提示词、可用模型、可用 Skill、知识库、运行端配置、权限策略和安全策略共同组成。

### 3.1 智能体类型

| 类型 | 说明 |
|---|---|
| 问答型智能体 | 面向知识库、文档、制度、FAQ 的问答 |
| 助手型智能体 | 面向办公、分析、写作、摘要、翻译等通用任务 |
| 工具型智能体 | 主要负责调用外部 API、系统动作和自动化工具 |
| 数据分析智能体 | 面向结构化数据查询、统计、图表和洞察生成 |
| 流程型智能体 | 作为流程节点参与业务编排和任务流转 |
| 审核型智能体 | 对内容、参数、结果、风险进行辅助审核 |
| 协同型智能体 | 与其他智能体协作完成复杂任务 |
| 自定义智能体 | 由平台用户按场景自行配置 |

### 3.2 智能体配置结构

```text
智能体
├─ 基础配置
│  ├─ 名称
│  ├─ 描述
│  ├─ 类型
│  ├─ 头像 / 图标
│  └─ 启用状态
├─ 能力配置
│  ├─ 系统提示词
│  ├─ 可用大模型
│  ├─ 可用知识库
│  ├─ 可用 Skill / Tool
│  └─ 输出格式要求
├─ 端侧配置
│  ├─ Web 端配置
│  ├─ 桌面端配置
│  ├─ 嵌入式对话框配置
│  └─ API 调用配置
├─ 权限配置
│  ├─ 可见范围
│  ├─ 可使用角色
│  ├─ 可调用工具
│  └─ 数据访问范围
└─ 治理配置
   ├─ 敏感词策略
   ├─ 人工确认策略
   ├─ 调用限额
   └─ 审计策略
```

### 3.3 Web 端与桌面端差异

| 配置项 | Web 端 | 桌面端 |
|---|---|---|
| 交互方式 | 浏览器页面、嵌入式对话框、业务系统侧边栏 | 桌面客户端、托盘助手、快捷键唤起、本地窗口 |
| 提示词 | 偏页面上下文、表单字段、业务对象、系统权限 | 偏本地文件、客户端状态、用户操作环境 |
| 上下文来源 | 当前页面、路由参数、接口数据、用户权限 | 本地文件、剪贴板、客户端缓存、设备状态 |
| 工具调用 | Web API、业务系统接口、知识库、在线工具 | 本地文件处理、桌面通知、本地应用联动、Web API |
| 权限控制 | 系统账号、角色、菜单、接口权限 | 客户端登录态、本地权限、设备绑定、接口权限 |
| 输出形式 | 页面回复、图表、表单填充、流程触发 | 弹窗回复、文件生成、任务提醒、本地应用联动 |

## 4. Prompt 管理

Prompt 是智能体行为控制的核心资产，应支持配置化、版本化、灰度化和可审计。

### 4.1 Prompt 类型

- 系统 Prompt
- 角色 Prompt
- 场景 Prompt
- 端侧 Prompt
- 工具调用 Prompt
- 输出格式 Prompt
- 安全约束 Prompt
- 多智能体协同 Prompt

### 4.2 管理能力

- Prompt 创建、编辑、复制和归档
- Prompt 版本管理
- Prompt 变量定义
- Prompt 模板复用
- Prompt 调试测试
- Prompt 发布审批
- Prompt 灰度启用
- Prompt 效果评估
- Prompt 调用审计

### 4.3 Prompt 变量示例

| 变量 | 说明 |
|---|---|
| `user.name` | 当前用户名称 |
| `user.role` | 当前用户角色 |
| `client.type` | 访问端类型，如 web、desktop、api |
| `page.context` | Web 页面上下文 |
| `file.context` | 桌面端或附件文件上下文 |
| `agent.name` | 当前智能体名称 |
| `conversation.summary` | 会话摘要 |
| `tool.results` | 工具调用结果 |

## 5. Skill / Tool 管理

Skill / Tool 是智能体可调用的确定性能力，用于把大模型从“生成回答”扩展到“查询、计算、处理、执行”。

### 5.1 Skill 分类

- 数据查询类：查询数据库、接口、表格、指标等结构化数据。
- 文件处理类：读取、解析、转换、生成 Word、Excel、PDF、图片等文件。
- 知识检索类：检索知识库、文档库、向量索引、FAQ。
- 内容生成类：生成报告、摘要、邮件、通知、图表说明。
- 系统动作类：创建工单、发送消息、回写系统、触发流程。
- 外部 API 类：封装第三方系统接口、SaaS API、企业内部服务。
- 代码执行类：执行受控脚本、数据分析代码、计算任务。
- 人工协同类：发起确认、审批、转人工、收集补充信息。

### 5.2 管理能力

- Skill 注册
- 参数 Schema 定义
- 返回值 Schema 定义
- 权限配置
- 调试测试
- 版本管理
- 启用 / 停用
- 绑定智能体
- 调用统计
- 异常日志
- 超时与重试配置
- 人工确认配置

### 5.3 Skill 注册字段

| 字段 | 说明 |
|---|---|
| Skill 名称 | 工具展示名称 |
| Skill 标识 | 系统内部唯一编码 |
| Skill 类型 | 数据查询、文件处理、外部 API、系统动作等 |
| 调用方式 | HTTP API、函数调用、脚本、消息队列、插件等 |
| 输入参数 | JSON Schema 或表单参数 |
| 输出结果 | JSON Schema、文本、文件、图表等 |
| 权限范围 | 用户、角色、部门、租户、智能体 |
| 超时设置 | 单次调用最长执行时间 |
| 重试策略 | 失败后是否重试及重试次数 |
| 审计策略 | 是否记录入参、出参、调用人和耗时 |

## 6. 知识库管理

知识库用于支撑 RAG 问答、文档检索、制度查询、材料生成和智能体上下文增强。

### 6.1 知识来源

- 文档上传
- 网页采集
- 数据库同步
- API 同步
- 手工录入
- FAQ 导入
- 企业知识库对接
- 对象存储文件同步

### 6.2 文档处理流程

```text
文件上传 / 数据同步
→ 格式解析
→ 文本清洗
→ 分段切片
→ 元数据提取
→ 向量化
→ 索引入库
→ 检索测试
→ 发布启用
```

### 6.3 管理能力

- 知识库创建
- 文档导入
- 文档解析
- 分段策略配置
- Embedding 模型配置
- 向量索引管理
- 检索测试
- 引用溯源
- 权限控制
- 更新同步
- 删除与重建索引
- 命中率与反馈统计

## 7. 专有对话管理

平台提供可嵌入、可审计、可追踪的专有 AI 对话能力。

### 7.1 对话能力

- 多轮对话
- 会话历史管理
- 会话分类和标签
- 上下文记忆
- 附件上传
- 文档问答
- 图表生成
- Tool 调用
- 数据查询
- 对话导出
- 对话共享
- 敏感词控制
- 人工接管

### 7.2 对话来源记录

- 会话来源端：Web / 桌面端 / 移动端 / API / 嵌入式组件
- 会话入口：页面、模块、客户端窗口、快捷键、第三方系统
- 用户身份
- 组织与租户
- 业务上下文
- 本地上下文
- 附件来源
- 调用 Skill 记录
- 模型调用记录
- 跨端同步状态

### 7.3 上下文治理

- 上下文长度控制
- 会话摘要压缩
- 敏感信息脱敏
- 附件权限校验
- 页面上下文授权
- Tool 结果裁剪
- 跨端上下文隔离
- 历史记忆启用 / 停用

## 8. 拖拽式流程编排

拖拽式流程编排是平台必做核心能力，不作为后续可选增强项。平台需要提供可视化流程设计器，用于编排数据、智能体、Skill、条件判断、人工确认和系统动作。

### 8.1 节点类型

- 开始节点
- 数据输入节点
- 数据转换节点
- Skill 调用节点
- 智能体分析节点
- 大模型问答节点
- 知识库检索节点
- 条件判断节点
- 循环节点
- 多智能体协同节点
- 人工确认节点
- 消息通知节点
- API 回调节点
- 结果输出节点
- 结束节点

### 8.2 编排能力

- 拖拽节点
- 连线配置
- 参数映射
- 条件分支
- 循环任务
- 定时调度
- 流程模板
- 流程版本管理
- 流程发布审批
- 流程运行日志
- 失败重试
- 人工干预
- 节点级权限控制
- 流程实例追踪

### 8.3 第一版必做边界

第一版流程编排先实现可运行的基础闭环，避免一开始追求完整 BPM 平台。

必做能力：

- 可视化拖拽画布
- 开始、结束、智能体、Skill、条件判断、人工确认、结果输出节点
- 节点参数配置
- 上下游参数映射
- 流程保存、发布和启停
- 手动触发流程
- API 触发流程
- 流程运行实例记录
- 节点运行状态记录
- 失败节点错误信息记录
- 人工确认节点待办与处理

第一版暂缓能力：

- 复杂循环
- 子流程
- 并行网关
- 高级定时调度
- 流程模板市场
- 可视化运行回放
- 复杂补偿事务

### 8.4 通用流程示例

```text
接收用户请求
→ 识别任务类型
→ 检索知识库
→ 调用相关 Skill
→ 智能体生成结果
→ 人工确认
→ 回写业务系统 / 输出结果
```

## 9. 多智能体协同

多智能体协同是平台必做核心能力，不作为后续可选增强项。第一版应优先实现可控、可审计、可复现的协同机制，避免过早引入不可解释的完全自治链路。

### 9.1 协同模式

- 主从模式：主智能体规划任务，专业智能体执行子任务。
- 会商模式：多个智能体分别给出分析意见，汇总智能体生成结论。
- 流水线模式：上一个智能体输出作为下一个智能体输入。
- 投票模式：多个智能体独立判断，系统聚合结果。
- 人机协同模式：智能体提出建议，用户审核、调整和确认。

### 9.2 协同治理

- 协同任务记录
- 子任务拆解记录
- 每个智能体输入输出留痕
- 工具调用留痕
- 冲突意见标记
- 汇总结论可追溯
- 人工确认节点
- 协同超时与终止策略

### 9.3 第一版必做边界

第一版多智能体协同先支持“编排式协同”，即通过流程或配置明确各智能体职责、执行顺序和汇总方式。

必做能力：

- 一个主智能体协调多个子智能体
- 子智能体按顺序执行
- 子智能体可绑定不同 Prompt、知识库和 Skill
- 上一个智能体输出可作为下一个智能体输入
- 汇总智能体生成最终结果
- 每个智能体的输入、输出、模型调用和 Tool 调用留痕
- 协同任务状态记录
- 协同失败时可查看失败智能体和失败原因
- 关键步骤支持人工确认

第一版暂缓能力：

- 智能体自主动态拉起任意智能体
- 大规模群体讨论
- 自动投票与复杂仲裁
- 长周期自治任务
- 跨租户智能体协同
- 完全开放的智能体插件生态

## 10. 大模型设置

平台支持国内外多个模型供应商、私有化模型、自定义模型接口、多模型接入、模型路由、调用限额和成本统计。

### 10.1 支持类型

- 公有云大模型
- 私有化部署大模型
- 本地推理模型
- Embedding 模型
- Rerank 模型
- 多模态模型
- 语音识别模型
- 语音合成模型
- OpenAI-compatible 自定义模型
- 自定义 HTTP 模型服务

### 10.2 支持供应商

国外供应商：

- OpenAI
- Azure OpenAI
- Anthropic
- Google Gemini
- AWS Bedrock
- Cohere
- Mistral AI

国内供应商：

- 阿里云通义千问
- 百度千帆 / 文心
- 智谱 AI
- Moonshot AI / Kimi
- DeepSeek
- 火山方舟 / 豆包
- 腾讯混元
- 讯飞星火
- MiniMax
- 零一万物

私有化与自定义：

- Ollama
- vLLM
- Xinference
- LocalAI
- 企业私有模型网关
- OpenAI-compatible 接口
- 自定义 HTTP 模型服务

### 10.3 配置项

- 模型供应商
- 供应商类型：国内、国外、私有化、自定义
- 接口协议：OpenAI-compatible、厂商原生 API、自定义 HTTP
- API 地址
- API Key
- 模型名称
- 厂商模型 ID
- 上下文长度
- 温度参数
- 最大输出长度
- 超时时间
- 并发限制
- 调用限额
- 成本统计
- 默认模型
- 备用模型
- 智能体绑定模型
- 可用租户 / 角色

### 10.4 路由策略示例

```text
普通问答 → 默认通用模型
知识库问答 → RAG + 通用模型
复杂推理 → 高性能推理模型
结构化抽取 → 低温度模型
图片理解 → 多模态模型
涉密场景 → 私有化模型
主模型失败 → 备用模型
```

## 11. 多用户访问与权限体系

平台支持多用户、多角色、多部门、多租户。

### 11.1 角色建议

- 系统管理员
- 平台管理员
- 租户管理员
- 智能体管理员
- Skill 管理员
- 知识库管理员
- 流程管理员
- 审核人员
- 普通用户
- 第三方系统账号

### 11.2 权限范围

- 菜单权限
- 数据权限
- 智能体权限
- Skill 权限
- 知识库权限
- 对话权限
- 流程权限
- API 权限
- 审批权限
- 配置权限
- 审计查看权限

### 11.3 安全能力

- 单点登录 SSO
- OAuth2 / OIDC
- LDAP / AD 对接
- 多租户隔离
- Web 智能体沙箱隔离
- API Token 管理
- 操作审计
- 登录审计
- 调用审计
- 敏感数据脱敏
- 密钥加密存储
- 访问频率限制
- 异常行为告警

## 12. 系统集成能力

### 12.1 对接 Web 系统

支持：

- REST API
- WebSocket
- JS SDK
- iframe 嵌入
- 单点登录
- Webhook 回调
- 统一身份认证
- 统一菜单集成
- 页面上下文注入

### 12.2 对接客户端应用

支持：

- PC 客户端
- 移动 App
- 小程序
- 平板终端
- 大屏系统
- 第三方业务系统

提供：

- OpenAPI
- WebSocket 实时通信
- SDK
- Token 鉴权
- 文件上传下载接口
- 对话接口
- 智能体调用接口
- Skill 调用接口
- 流程触发接口

### 12.3 AI 对话框调用示例

```javascript
AgentChat.init({
  appId: "business-system",
  userId: "u1001",
  token: "xxxx",
  agentId: "assistant-default",
  context: {
    page: "order-detail",
    objectId: "obj-001",
    source: "web"
  }
});
```

## 13. 推荐技术架构

### 13.1 前端

- Vue 3
- TypeScript
- Vue Vben Admin
- Ant Design Vue
- LogicFlow / X6
- ECharts
- Monaco Editor
- Markdown 渲染组件

### 13.2 后端

- Java Spring Boot / Spring Cloud
- Python FastAPI 作为 AI 服务层
- API Gateway
- WebSocket / SSE
- Redis
- RabbitMQ / Kafka
- MinIO
- PostgreSQL / MySQL
- Elasticsearch / OpenSearch
- Milvus / pgvector / Qdrant

### 13.3 部署运维

- Docker
- Kubernetes
- Helm
- Nginx
- Prometheus + Grafana
- ELK / Loki
- 配置中心
- 密钥管理服务

## 14. 核心数据库表建议

- 用户表
- 角色表
- 权限表
- 部门表
- 租户表
- 智能体表
- 智能体端侧配置表
- Prompt 表
- Prompt 版本表
- Skill 表
- Skill 版本表
- 智能体 Skill 绑定表
- 知识库表
- 知识库文档表
- 文档分段表
- 向量索引记录表
- 流程定义表
- 流程节点表
- 流程版本表
- 流程运行实例表
- 流程节点运行记录表
- 对话会话表
- 对话消息表
- 对话附件表
- Tool 调用记录表
- 大模型配置表
- 大模型调用记录表
- API 应用表
- API Token 表
- Webhook 配置表
- 审批任务表
- API 调用日志表
- 审计日志表

## 15. 通用能力场景

### 15.1 知识库问答

```text
用户提问
→ 权限校验
→ 检索知识库
→ 重排检索结果
→ 大模型生成回答
→ 返回引用来源
→ 记录会话与反馈
```

### 15.2 工具调用助手

```text
用户提出任务
→ 智能体识别意图
→ 选择可用 Skill
→ 参数补全与确认
→ 调用 Skill
→ 解释执行结果
→ 记录调用日志
```

### 15.3 流程自动化

```text
触发流程
→ 获取输入数据
→ 执行条件判断
→ 调用智能体或 Skill
→ 人工确认
→ 输出结果 / 回调系统
→ 归档运行记录
```

### 15.4 嵌入式 AI 对话框

```text
第三方系统打开页面
→ AI 对话框注入页面上下文
→ 用户发起提问
→ 智能体结合页面对象、知识库和工具回答
→ 必要时触发流程或回写系统
```

## 16. 建设路线

### 16.1 第一阶段：平台基础版

周期建议：3-4 个月。

- 用户登录与基础权限
- 智能体管理
- Prompt 管理
- Skill 注册与调试
- 大模型配置
- 基础 AI 对话
- 知识库问答
- 文件上传与文档解析
- 拖拽式流程编排基础版
- 编排式多智能体协同
- Tool 调用记录
- OpenAPI 基础框架

目标：形成可运行、可配置、可编排、可协同、可审计的通用智能体平台底座。

### 16.2 第二阶段：能力增强版

周期建议：3-4 个月。

- 流程编排增强
- 多智能体协同增强
- Web / 桌面端智能体差异化配置
- 对话上下文管理
- Prompt 版本与灰度
- Skill 版本管理
- 复杂权限与多租户
- 嵌入式 AI 对话框
- API / Webhook 集成

目标：支撑多系统嵌入、流程自动化和可控的多智能体协同。

### 16.3 第三阶段：平台化运营版

周期建议：2-3 个月。

- JS SDK
- 客户端 SDK
- 插件市场 / Skill 市场
- 运营统计与效果评估
- 成本统计与调用限额
- 审计与安全增强
- 监控运维体系
- 私有化部署方案

目标：支持平台化推广、企业级复用和多场景扩展。

## 17. MVP 优先级

第一版建议优先建设：

1. 用户登录与基础权限
2. 大模型配置
3. 智能体管理
4. Prompt 管理
5. Skill 注册与调试
6. 基础 AI 对话
7. 知识库问答
8. 文件上传与文档解析
9. 拖拽式流程编排基础版
10. 编排式多智能体协同
11. Tool 调用记录
12. OpenAPI 基础接口
13. 基础审计日志

第一版暂不强依赖：

- 行业专用模型
- 复杂多智能体自治
- 桌面端深度本地操作
- 高级流程编排能力
- 插件市场
- 多租户复杂计费

## 18. 最终交付物

- 通用智能体平台管理端
- 智能体管理中心
- Prompt 管理中心
- Skill / Tool 管理中心
- 知识库管理系统
- AI 对话框组件
- 大模型配置中心
- 对话与上下文管理能力
- 拖拽式流程编排器
- 多智能体协同引擎
- OpenAPI 服务
- JS SDK / 客户端 SDK
- 用户权限与审计系统
- 调用统计与监控面板
- 部署文档
- 接口文档
- 运维监控方案

## 19. 智能体运行与生态接入深化方案

本章对多用户运行隔离、人工审批、Deep Agents、Agent Harness、RAG 知识驱动和外部专业智能体接入进行统一设计。总体原则是：平台负责身份、权限、资源和安全治理，Agent Harness 负责运行编排，Deep Agents 等引擎负责推理与任务循环，知识库和外部智能体作为受控能力接入。

### 19.1 总体运行架构

```text
Web / 桌面端 / 第三方系统
            ↓
API 网关、认证、租户与项目权限
            ↓
PlatformAgentHarness
  ├─ Agent、模型、Prompt、Skill、MCP 配置解析
  ├─ RAG 检索与知识证据组装
  ├─ 风险策略、人工审批和审计
  ├─ 沙箱分配、凭据注入和资源限额
  └─ Deep Agents / LangGraph / 自定义运行引擎
            ↓
Skill、MCP、模型服务、外部智能体网关
            ↓
GIS、遥感、水文、调度优化和控制等专业服务
```

平台不应将某一种 Agent 框架直接暴露给业务层。建议定义稳定的 Harness 接口，使 Deep Agents 成为首选运行引擎，同时为 LangGraph 或自研引擎保留替换空间。

```python
class AgentHarness(Protocol):
    async def invoke(self, request: AgentRunRequest) -> AgentRunResult: ...
    async def resume(self, approval: ApprovalDecision) -> AgentRunResult: ...
    async def cancel(self, run_id: str) -> None: ...
```

### 19.2 多用户隔离与运行环境

Web 多用户场景不建议为每个用户长期保留一个容器。推荐采用“数据域长期隔离、任务环境按需创建”的方式：

- 租户、项目、用户、智能体、会话和知识库均带有明确的数据归属与访问范围。
- 每次代码、脚本、文件转换或不可信工具任务分配临时沙箱，任务结束后销毁或按审计策略短期保留。
- 普通对话和只读检索可共享运行集群，但必须在数据库、缓存、向量索引、对象存储和检查点中强制使用租户命名空间。
- 模型密钥、MCP 凭据和外部服务令牌由平台按任务临时注入，不写入 Prompt、会话或用户工作区。
- 沙箱限制 CPU、内存、磁盘、运行时间、进程数、网络出口和可挂载目录。
- 生产环境禁止使用没有隔离边界的本地 Shell 后端。

隔离键建议至少包含：

```text
tenant_id / project_id / user_id / agent_id / conversation_id / run_id
```

检查点、长期记忆、文件路径和对象存储前缀均由服务端生成，不能直接信任客户端提交的隔离键。

### 19.3 审批与风险策略

多用户 Web 化不等于取消人工确认。审批策略应根据动作风险，而不是根据 Web 或桌面运行形态简单决定。最终策略取平台、租户、项目、用户角色、工具风险和智能体配置中最严格的结果。

建议风险等级如下：

| 等级 | 典型行为 | 默认策略 |
| --- | --- | --- |
| 低风险 | 知识检索、只读查询、格式转换 | 自动执行并记录 |
| 中风险 | 创建分析任务、生成成果文件、写入草稿数据 | 按租户策略确认 |
| 高风险 | 修改业务数据、发布图层、调用外部写接口 | 必须人工确认 |
| 关键风险 | 水库、闸门、泵站等生产控制命令 | 双人复核或业务审批流 |

审批记录必须绑定调用工具、参数摘要、知识依据、数据版本和执行环境。审批后如关键参数、目标对象或数据版本发生变化，应使原审批失效并重新确认。平台应支持批准、编辑后批准、拒绝和补充信息等处置方式。

### 19.4 Deep Agents 与 Platform Harness

Deep Agents 可作为 Harness 内部的 Agent 运行时，负责 Agent 循环、计划生成、Skill 使用、上下文文件系统、子任务组织以及 LangGraph 检查点和中断恢复。平台仍负责：

- 用户认证、多租户和项目权限。
- Agent、模型、多个 Skill、MCP 和知识库的配置解析。
- 风险策略、人工审批、凭据、沙箱和审计。
- 外部智能体注册、能力发现、调用限额和运行追踪。
- 对话、任务、检查点和成果文件的生命周期管理。

Deep Agents 的人工中断能力可用于实现批准、编辑、拒绝和补充信息流程；持久化状态必须使用包含租户边界的线程标识。文件存储可按用途路由到会话临时存储、用户长期记忆或项目对象存储。Deep Agents 的文件权限不能替代 MCP、自定义工具和 Shell 沙箱的安全控制。

建议运行模块结构：

```text
ai-services/
  agent-runtime/
    harness.py
    agent_factory.py
    runtime_context.py
    tool_resolver.py
    skill_resolver.py
    knowledge_resolver.py
    external_agent_resolver.py
    backend_factory.py
    approval_middleware.py
    checkpoint.py
```

### 19.5 RAG 知识驱动智能体

RAG 可以驱动智能体回答、规划和行为建议，但检索到的自然语言内容不能直接取得工具执行权。建议将知识分为业务知识库、项目知识库、Agent 专属知识库和用户或会话临时资料，并支持以下三种用途：

1. 回答驱动：检索标准、规程、设备资料和历史记录，生成带来源引用的答案。
2. 决策驱动：根据调度规程、应急预案、阈值和约束生成候选方案。
3. 行为驱动：将审核后的知识规则转换为结构化策略，建议调用指定 Skill、MCP 或外部智能体。

行为规则应包含规则标识、适用条件、建议能力、参数约束、风险等级、生效时间、失效时间、来源文档及版本。执行链路为：

```text
检索知识 → 生成计划 → 校验证据与规则
→ 解析 Skill/MCP/外部智能体调用 → 权限与风险检查
→ 必要时人工审批 → 沙箱或远程服务执行 → 审计留痕
```

知识库管理需要增加用途、数据权限、混合检索策略、最低相关度、引用要求、规则审核状态、冲突处理和数据新鲜度等配置。检索文档按不可信输入处理，防止提示注入；知识内容不能提升用户权限或绕过审批。每次决策应保存引用片段、文档版本、检索得分、模型结论、工具参数和审批结果。

### 19.6 外部专业智能体接入

GeoLibre、GIS Agent、遥感 Agent、水文 Agent、调度优化 Agent 等专业能力统一通过“外部智能体网关”接入 Harness。平台根据能力形态支持以下协议：

- MCP：适合图层查询、空间分析、坐标转换、地图渲染等工具型能力。
- HTTP / OpenAPI：适合已有稳定 REST API 的确定性服务。
- A2A：适合拥有独立会话、规划、任务状态和产物管理的完整智能体。
- 异步任务或消息队列：适合大栅格、遥感影像、洪水演进等长时间任务。
- 本地沙箱适配：仅用于确需本地执行且可被严格隔离的专业程序。

同一个专业系统可以同时提供能力层和智能体层。例如，简单缓冲区分析直接调用 GeoLibre 的 MCP/HTTP 工具；“分析水库下游六小时潜在淹没范围并生成专题图”则委派给具有任务规划能力的 GIS Agent。

统一适配接口建议如下：

```python
class ExternalAgentAdapter(Protocol):
    async def discover(self) -> list[Capability]: ...
    async def invoke(
        self,
        task: ExternalAgentTask,
        context: RuntimeContext,
    ) -> ExternalAgentRun: ...
    async def get_status(self, run_id: str) -> ExternalAgentRun: ...
    async def cancel(self, run_id: str) -> None: ...
```

外部智能体注册信息至少包括协议、服务地址、能力清单、输入输出 Schema、认证引用、同步或异步模式、超时、重试、并发限制、租户与项目范围、风险等级和审批策略。管理端应支持注册、导入、连接测试、启停、能力同步、白名单、Agent 绑定、运行记录和成果文件管理。

### 19.7 GeoLibre / GIS 接入约束

GIS 类外部智能体除通用安全要求外，还应满足：

- 只能访问当前租户和项目授权的图层、服务与空间范围。
- 请求和结果必须显式记录坐标参考系，例如 `EPSG:4490` 或 `EPSG:4547`，禁止静默猜测。
- 输出优先采用 GeoJSON、COG、GeoPackage、矢量瓦片、栅格瓦片或地图截图等结构化成果。
- 大文件通过对象存储短时签名地址传递，不进入 LLM 上下文。
- 写图层、覆盖数据、发布服务和共享成果属于高风险操作。
- 记录数据版本、时间范围、坐标系、算法版本、参数和来源图层。
- 地图渲染仅用于展示，控制决策必须回查原始数据和模型结果。

典型业务链路：

```text
用户提出下游风险分析请求
→ Harness 检索项目知识和调度规程
→ GIS Agent 获取授权图层与实时数据
→ 提交空间分析异步任务
→ 返回 GeoJSON、COG 或专题图成果
→ Web 地图加载成果图层
→ 平台智能体解释结果并引用数据来源
→ 如需生成控制方案，再进入高风险审批流程
```

### 19.8 外部智能体网关设计

外部智能体网关不是简单的 HTTP 转发代理，而是运行时的统一协议与治理层。所有外部专业能力必须由 `PlatformAgentHarness` 经网关调用，不能由业务 Agent 绕过网关直接访问外部服务。

```text
PlatformAgentHarness
        ↓
External Agent Gateway
  ├─ 注册、能力发现与健康检查
  ├─ MCP / OpenAPI / A2A / Async Job 协议适配
  ├─ 租户权限、能力白名单与风险策略
  ├─ 凭据引用、超时、重试、限流与幂等
  ├─ 同步/异步任务状态和安全回调
  ├─ 事件标准化、调用追踪与审计
  └─ Artifact 成果文件登记
        ↓
GeoLibre、GIS、水文、遥感、调度优化等专业智能体
```

外部服务同步出的 MCP Tool、OpenAPI Operation、A2A Skill 均转换为统一 `Capability`。能力至少记录编码、输入输出 Schema、同步或异步方式、读写属性、风险等级、幂等性、超时和启停状态。Agent 绑定具体能力白名单，不默认获得整个外部服务的全部能力。实际可调用集合取服务、租户、项目、Agent 版本和当前用户允许能力的交集。

网关提供统一接口：

```python
class ExternalAgentGateway(Protocol):
    async def discover(
        self,
        external_agent_id: str,
        context: RuntimeContext,
    ) -> list[Capability]: ...

    async def invoke(
        self,
        request: ExternalAgentRequest,
        context: RuntimeContext,
    ) -> ExternalAgentRun: ...

    async def get_status(
        self,
        run_id: str,
        context: RuntimeContext,
    ) -> ExternalAgentRun: ...

    async def cancel(
        self,
        run_id: str,
        context: RuntimeContext,
    ) -> None: ...
```

`tenant_id`、`project_id`、`user_id`、`conversation_id` 和 `trace_id` 由服务端运行上下文注入，不能信任模型或浏览器直接提交的安全上下文。提交任务前依次执行 Schema 校验、数据权限校验、有效风险计算、参数规范化和摘要计算；高风险调用先创建审批，中断通过后再次校验摘要再提交外部任务。

异步任务统一使用 `created`、`submitted`、`running`、`waiting_input`、`succeeded`、`failed`、`cancelled` 和 `timed_out` 状态。网关保存平台 Run 与外部任务 ID 的映射，并用 `(external_agent_id, idempotency_key)` 防止重试产生重复任务。Webhook 回调必须使用独立密钥、HMAC 签名、时间戳、防重放和幂等处理；回调只能更新任务和成果，不能直接触发未经策略校验的高风险后续操作。

大型 GIS 成果统一登记为 `Artifact`，至少记录类型、对象存储路径、校验和、大小、CRS、包围盒、来源数据版本、算法参数和生成时间。模型只获取摘要和元数据，前端通过短时签名地址加载 GeoJSON、COG、GeoPackage、MVT 或图片成果。

建议代码结构：

```text
backend/app/
  external_agents/
  external_runs/
  artifacts/

ai-services/
  external-agent-gateway/
    gateway.py
    registry.py
    policy_enforcer.py
    task_manager.py
    event_normalizer.py
    artifact_service.py
    callback_handler.py
    adapters/
      mcp.py
      openapi.py
      a2a.py
      async_job.py
```

### 19.9 专业智能体工作区设计

GIS、水文、调度等专业智能体不能只使用纯文本问答界面。前端应采用“通用对话壳 + 专业工作区插件”，共享会话、消息、工具调用、审批和成果文件能力，专业插件负责地图、过程线、模型参数或调度方案交互。

```text
智能体工作台
├─ 通用区域
│  ├─ 会话列表、消息流和输入框
│  ├─ 工具调用、任务进度和审批
│  └─ 引用与成果文件
└─ 专业工作区
   ├─ GIS 地图工作区
   ├─ 水文过程线工作区
   ├─ 调度方案工作区
   └─ 模型参数与报告工作区
```

GIS 桌面布局以地图为主、对话为辅，包含地图主视图、图层树、图例、空间选择、绘制与量算、要素属性、时间控制、异步分析任务和成果面板。移动端采用“地图、对话、结果”页签切换，避免狭窄视口同时挤压地图和消息区。

GIS 第一版建议包括 `GisWorkspace`、`MapCanvas`、`LayerPanel`、`FeatureInspector`、`SpatialSelectionTool`、`DrawingToolbar`、`MapContextProvider`、`GisArtifactLayer`、`MapLegend`、`AnalysisTaskPanel`、`GisApprovalPreview` 和 `TimeController`。平台独立建设时优先评估 OpenLayers；若现有业务系统已使用 OpenLayers、MapLibre、Cesium 或 ArcGIS，则通过宿主适配器复用现有地图。

地图上下文以结构化数据进入 Agent，只传递当前工程、CRS、地图范围、授权图层 ID、选中要素 ID和时间范围。后端必须重新校验图层和要素权限。用户说“分析这个水库”时，可由当前选中要素消解指代，但不能让模型从截图或自然语言猜测业务对象。

对话和地图双向联动：

```text
地图框选或点选对象
→ 前端产生 workspace.context.changed
→ 发送消息时附加结构化上下文
→ GIS Agent 完成分析并登记 Artifact
→ Run 发出 artifact.ready 和 workspace.command
→ GIS 工作区加载成果、缩放定位并高亮
```

平台支持两种使用方式：平台内加载完整专业工作区；嵌入已有 GIS 系统时只加载 Chatbox，并通过 `MapHostAdapter` 获取地图上下文和执行受控命令。这样平台不依赖宿主的具体地图引擎。

### 19.10 Deep Agents 与插件调用边界

平台中的“插件”分为运行时能力插件和前端工作区插件：

| 插件类型 | 示例 | Deep Agents 调用方式 |
| --- | --- | --- |
| 运行时能力 | Skill、MCP Tool、RAG Retriever、外部 Agent | 由 Harness 解析 Agent 版本后注册为允许工具 |
| 前端工作区 | GIS 地图、水文过程线、调度方案 Vue 组件 | 不能直接执行，只能通过受控事件请求操作 |

Deep Agents 不自行发现任意插件，也不能安装前端组件。Agent 发布版本明确绑定模型、多个 Skill、MCP 工具、知识库、外部 Agent 能力和专业工作区；Harness 再结合租户权限、启停状态、版本和白名单，只向本次 Deep Agents 实例注入允许的运行时工具。

前端插件由 Agent 发布配置决定：

```json
{
  "workspace": {
    "type": "gis",
    "plugin_id": "builtin.gis-workspace",
    "plugin_version": "1.0.0",
    "config": {
      "default_crs": "EPSG:4547",
      "allowed_layer_types": ["geojson", "cog", "wms", "wmts", "mvt"]
    }
  }
}
```

Deep Agents 不能生成 JavaScript 或直接调用地图实例。需要操作界面时，调用平台内置的工作区命令工具，由后端对工作区类型、命令、参数、Artifact 和权限进行白名单校验，再通过 Run 事件发送给前端。GIS 插件二次校验事件并通过地图适配器执行。

允许的 GIS 工作区命令可包括：

```text
layer.add / layer.remove / layer.visibility.set / layer.opacity.set
map.zoom_to / map.center
feature.highlight / feature.select
timeline.set / panel.open
```

永久禁止任意浏览器 JavaScript、任意 URL 加载、直接 SQL 和运行时安装新插件。`layer.add` 只能引用平台登记的 `artifact_id` 或已授权图层 ID。写图层、覆盖数据和发布服务仍属于运行时能力调用，必须经过网关和审批，不能伪装成低风险 UI 命令。

建议维护两套注册表：`runtime_capabilities` 管理 Skill、MCP、Retriever 和外部 Agent 能力；`workspace_plugins` 管理前端入口、版本、配置 Schema、支持命令和事件。两者通过映射表声明某个工作区可以配合哪些运行时能力和 UI 命令。

统一 Run 事件至少包括：

```text
workspace.command
workspace.command.completed
workspace.context.changed
artifact.ready
task.progress
approval.required
```

### 19.11 当前项目前端改造

当前 AI 对话页应改为通用智能体工作台外壳，根据 Agent 发布版本的 `workspace.type` 动态加载插件，而不是为每个专业 Agent 复制一套会话页面。建议新增：

```text
frontend/src/workspaces/
  registry.ts
  types.ts
  common/
  gis/
    GisWorkspace.vue
    MapCanvas.vue
    LayerPanel.vue
    FeatureInspector.vue
    DrawingToolbar.vue
    AnalysisTaskPanel.vue
    GisApprovalPreview.vue
    MapLegend.vue
    TimeController.vue
    map-command-handler.ts
    map-context-provider.ts
    artifact-layer-factory.ts
```

现有 Agent 管理功能增加工作区类型、插件版本和专业配置，并增加外部智能体及能力白名单选择；AI 对话功能接入真实 Run 事件，处理 `artifact.ready`、`workspace.command`、任务进度和审批卡片；后端 Agent Schema 增加 `workspace`、`external_agent_bindings`、`mcp_bindings` 和 `knowledge_bindings`。

第一阶段优先实现通用工作台外壳、GIS 插件注册机制、结构化地图上下文、Artifact 加载和白名单命令。二维地图能力稳定后，再按明确业务需求建设 Cesium 三维工作区。

### 19.12 建设落地顺序

1. 建立 `PlatformAgentHarness` 接口、运行上下文、检查点和调用审计模型。
2. 将现有 Agent、多个 Skill、MCP、模型和 Prompt 配置接入解析器。
3. 接入 Deep Agents，完成调用、中断、恢复和取消能力。
4. 建立租户安全的临时沙箱、凭据注入与资源配额。
5. 接入 RAG 检索工具和知识证据链，后续再启用审核后的结构化行为规则。
6. 建设外部智能体注册中心与 MCP、HTTP、A2A、异步任务适配器。
7. 建设通用智能体工作台和专业工作区插件注册机制。
8. 以 GeoLibre / GIS 为首个专业智能体试点，打通结构化地图上下文、异步任务、Artifact、地图命令和对象存储。
9. 扩展水文、遥感、调度优化和控制智能体，并复用工作区插件、外部网关和高风险业务审批流。

验收测试至少覆盖 Web / 桌面 Prompt 选择、多个 Skill 加载、租户与会话隔离、高风险工具中断、审批参数防篡改、审批后恢复、未授权恢复拒绝、知识来源引用、失效规则拦截、外部任务幂等与安全回调、GIS 数据权限、坐标系校验、异步任务取消、工作区插件加载、地图上下文校验、Artifact 加载、非法 UI 命令拦截以及地图与对话双向联动。
